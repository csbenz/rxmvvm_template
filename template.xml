<?xml version="1.0"?>
<template
    format="5"
    revision="3"
    name="rx + mvvm architecture"
    description="Creates a Fragment, ViewModel etc.">

    <category value="Other" />

    <parameter
        id="prefix"
        name="Prefix identifier"
        type="string"
        constraints="class|nonempty|unique"
        default="Example"
        help="The prefix id to use for this mvvm template" />

    <parameter
        id="fragment_layout"
        name="Fragment layout file name"
        type="string"
        constraints="class|nonempty|unique"
        default="fragment_example"
        help="The name of the file created for the fragment layout" />

    <parameter
        id="event"
        name="Event name"
        type="string"
        constraints="class|nonempty|unique"
        default="DummyEvent"
        help="The name of an UiEvent to setup in the ViewModel" />

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
